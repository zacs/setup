---
- name: install CLI tools
  homebrew: name={{ item }} state=present
  with_items:
    - git
    - s3cmd
    - brew-cask
    - wget
    - hub
    - python
    - ruby
    - node
    - archey
    - the_platinum_searcher
    - brew-gem
    - brew-pip
    - hub
    - yuicompressor
    - watchman
    - ffmpeg
    - youtube-dl
    - zsh
  when: ansible_os_family == 'Darwin'

- name: install gem tools
  gem: name={{ item }} state=present
  with_items:
    - jekyll
    - kramdown
  when: ansible_os_family == 'Darwin'

- name: install photoshop cs5 (skip during CI builds)
  homebrew_cask: name={{ item }} state=present
  with_items:
    - adobe-photoshop-cs5
  when: ansible_os_family == 'Darwin' and ansible_env.CLI is not defined

- name: install binary casks
  homebrew_cask: name={{ item }} state=present
  with_items:
    - 1password
    - azure-cli
    - alfred
    - archiver
    - arq
    - atom
    - beamer
    - charles
    - chromium
    - doxie
    - dropbox
    - dropletmanager
    - flash
    - flux
    - google-chrome-canary
    - ios7-screensaver
    - istat-menus
    - java
    - mailbox
    - media-converter # could make its own role and clone the added presets i use
    - openemu-experimental
    - sidestep
    - slate
    - soulver
    - tagger
    - transmission
    - transmit
    - virtualbox
    - visual-studio-code
    - vlc
  when: ansible_os_family == 'Darwin'

- name: apply OS X for hackers settings
  script: osx
  when: ansible_os_family == 'Darwin'

- name: create dirs
  file: name={{ ansible_env.HOME }}/{{ item }} state=directory
  with_items:
    - 'Code'
    - 'Code/scratch'

# TODO: Add apt-get calls for Ubuntu
