---
- name: install CLI tools
  homebrew: name={{ item }} state=present
  with_items:
    - git
    - s3cmd
    - brew-cask
    - wget
    - heroku-toolbelt
    - hub
    - python
    - ruby
    - node
    - archey
    - the_platinum_searcher
    - brew-gem
    - brew-pip
    - hub
    - imagemagick
    - yuicompressor
    - watchman
    - ffmpeg
    - youtube-dl
  when: ansible_os_family == 'Darwin'

- name: install gem tools
  gem: name={{ item }} state=present
  with_items:
    - jekyll
    - kramdown
  when: ansible_os_family == 'Darwin'

- name: install binary casks
  homebrew_cask: name={{ item }} state=present
  with_items:
    - 1password
    - azure-cli
    - adobe-creative-cloud
    # - adobe-Photoshop-CS6 # no formula in caskroom/versions yet
    - alfred
    - archiver
    - arq
    - audio-hijack-pro
    - base
    - beamer
    - keepingyouawake
    - charles
    - chromium
    - cloak
    - dnscrypt
    - doxie
    - dropbox
    - dropletmanager
    - flash
    - flux
    - gitbox
    - google-chrome-canary
    - ios7-screensaver
    - istat-menus
    - java
    - kindle
    - kindlegen
    - mailbox
    - media-converter # could make its own role and clone the added presets i use
    - openemu-experimental
    - plug
    - sequel-pro
    - sidestep
    - soulver
    - spotify
    - steam
    - steermouse
    - sublime-text3
    - tagger
    - trailer
    - transmission
    - transmit
    - unison # necessary?
    - vagrant
    - virtualbox
    - vlc
  when: ansible_os_family == 'Darwin'

- name: apply OS X for hackers settings
  shell: bash {{ item }}
  with_file: osx
  when: ansible_os_family == 'Darwin'

- name: create code dir
  file: name={{ item }} state=directory
  with_items:
    - '{{ home }}/Code'

# TODO: Add apt-get calls for Ubuntu
